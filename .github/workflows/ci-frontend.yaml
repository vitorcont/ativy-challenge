name: Frontend Deploy Automation

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "dashboard/*"
jobs:
  build:
    runs-on: self-hosted
    steps:
      - run: ls
      - run: docker compose up
